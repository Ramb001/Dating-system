import NavigationBar from "../../components/NavigationBar";
import styles from "./Home.module.scss";

function Home() {
  const user = window.localStorage.getItem("user_id") || null;

  return <></>;
}

export default Home;
